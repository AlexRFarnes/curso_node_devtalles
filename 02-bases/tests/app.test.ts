describe("App", () => {
  it("should be 30", () => {
    //    Arrange
    const number = 10;
    const number2 = 20;

    //    Act
    const result = number + number2;

    //    Assert
    expect(result).toBe(30);
  });
});
